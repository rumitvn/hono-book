{
  "name": "hono-hello",
  "type": "module",
  "private": true,
  "description": "Minimal Hono app — the runnable artifact for the Hono Internals Handbook labs",
  "scripts": {
    "dev": "bun run --hot index.ts",
    "start": "bun run index.ts"
  },
  "dependencies": {
    "hono": "4.12.25"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
